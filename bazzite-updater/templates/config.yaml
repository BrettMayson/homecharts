apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bazzite-updater.fullname" . }}
data:
  config.yaml: |
    machines:
      {{ with .Values.machines }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
